import { relate, destruct } from '../src/fire-methods';

describe('Relate', () => {
	it('', () => {
		expect(false).to.be.true;
	});
});


describe('Destruct', () => {
	it('', () => {
		expect(false).to.be.true;
	});
});