var expect = chai.expect;




describe('DOM tests - Signup form', () => {


    it('Form exists in the DOM', done => {
        setTimeout(() => {
            expect(true).to.equal(true);
            done()
        }, 2000)
    }).timeout(600000);

});